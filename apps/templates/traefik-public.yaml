apiVersion: {{ .Values.apiVersion }}
kind: Application
metadata:
  name: {{ .Values.pubTraefik.appName }}
  namespace: {{ .Values.argoNamespace }}
spec:
  destination:
    namespace: {{ .Values.pubTraefik.spec.destination.namespace }}
    server: {{ .Values.pubTraefik.spec.destination.server }}
  source:
    repoURL: {{ .Values.pubTraefik.spec.source.repoURL }}
    targetRevision: {{ .Values.pubTraefik.spec.source.targetRevision }}
    chart: {{ .Values.pubTraefik.spec.source.chart }}
    helm:
      values: |
    {{ .Files.Get "values/pubTraefik.yaml" | nindent 8 }}

  project: {{ .Values.pubTraefik.spec.project }}
  syncPolicy:
    {{- .Values.pubTraefik.spec.syncPolicy | toYaml | nindent 4 }}
