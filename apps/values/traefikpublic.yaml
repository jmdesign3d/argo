providers:
  kubernetesGateway:
    enabled: true
gatewayClass:
  enabled: true
  name: traefik-public
gateway:
  enabled: false
deployment:
  replicas: 3
nodeSelector:
  kubernetes.io/os: linux
  kubernetes.io/arch: amd64
service:
  externalTrafficPolicy: Local
  annotations:
    service.beta.kubernetes.io/vultr-loadbalancer-backend-protocol: https
